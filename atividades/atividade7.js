/* 

a) simule um processo de conferencia de mercadorias no backend

b) crie um loop (for, while) que conte de 1 a 10

c) a cada iteracao, o console deve exibir: "item #(X) verificado e registrado no inventario"

*/

for(let num = 1; num <= 10; num++)
{
    console.log(`Item ${num} verificado e registrado no inventario`);
}